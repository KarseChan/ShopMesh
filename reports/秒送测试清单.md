# 秒送垂类 · 手工测试清单

> 逐条测,勾 □。分「前端(浏览器)」「命令行(最稳,不依赖 LLM/前端)」两类。
> 遇到不符合预期的,记到最下方「问题记录」。

---

## 0. 前置检查(先确认环境都起了)

- [ ] 依赖容器:`docker compose -f docker-compose.infra.yml up -d`
      (pg 15432 / redis 16379 / qdrant 16336 / rabbitmq 5672 / ollama 11434 + bge-m3)
- [ ] mock 数据 + 索引(改过 mock 要重跑):
  ```bash
  PYTHONPATH=. python scripts/generate_mock_merchants.py
  PYTHONPATH=. python scripts/generate_mock_dishes.py
  PYTHONPATH=. python scripts/build_merchant_index.py
  ```
- [ ] 后端:`PYTHONPATH=. python -m uvicorn src.api.chat:app --port 9000`
      → `curl http://127.0.0.1:9000/api/health` 返回 `{"status":"ok"}`
- [ ] 前端:`cd frontend && npm run dev` → http://localhost:3000,登录 `demo/demo1234`
- [ ] (P3 组单 assemble 需要)`.env` 里 LLM 配置可用(MiniMax)
- [ ] (履约自动推进 / 订单超时取消需要)Celery:
      `python -m celery -A src.tasks worker -Q memory,cleanup,default,events --pool=solo`
      + beat(自动履约每 60s):`python -m celery -A src.tasks beat`

> 提示:mock 门店的**正例已设 24 小时营业**,所以就近检索任意时刻都能召回;
> 「营业中过滤」由两个显式打烊负例证明,**最好用命令行(F 组)固定时间验证**。

---

## A. 就近检索(P1)· 前端

| # | 输入 | 预期 | □ |
|---|------|------|---|
| A1 | `附近奶茶店30分钟送到` | 6 张门店卡片,按送达速度排序(蜜雪冰城 0.4km/15min 第一,2.2km/30min 垫底);卡片含品类/评分/距离/ETA/配送费/人均/理由 | □ |
| A2 | `附近快餐 半小时 人均30以内` | 只出快餐门店,且人均 ≤30、ETA ≤30 | □ |
| A3 | `附近奶茶店5分钟送到` | 文本提示「没找到能 5 分钟送到的奶茶」(优雅降级,**不返回不达标门店**) | □ |
| A4 | `附近有什么奶茶` | 出奶茶门店(无时效约束,数量可能更多) | □ |

## B. 意图路由边界(证明「哪些不该进 agent」)

| # | 输入 | 预期 | □ |
|---|------|------|---|
| B1 | `推荐几款好喝的奶茶` | 走**商品推荐**(商品卡 ProductCard),**不是**门店卡片 —— 未被秒送劫持 | □ |
| B2 | `再来一单`(还没下过单时) | 文本「没有找到可再来一单的历史订单」 | □ |
| B3 | `帮我组个30块的奶茶套餐` | 走 LLM 组单(见 D 组);会稍慢 | □ |

## C. 点单闭环(P2)· 前端

| # | 操作 | 预期 | □ |
|---|------|------|---|
| C1 | A1 后,点某门店「查看菜单 · 点单」 | 展开该店菜单(3~5 道,带价格 + 「加入」) | □ |
| C2 | 点某菜品「加入」 | 右下购物车抽屉弹出,出现该菜品、数量、合计 | □ |
| C3 | 再加 1~2 道 / 调数量 / 删除 | 购物车合计随之更新 | □ |
| C4 | 购物车点「确认下单」 | 下单成功,返回订单号;购物车清空 | □ |
| C5 | 「📦 我的订单」 | 看到刚下的订单,状态「待支付」,金额正确 | □ |
| C6 | 订单里点「支付」(若有)/ 或走购物车支付流程 | 状态变「已支付」 | □ |
| C7 | 已支付订单点「退款」 | 状态变「已退款」 | □ |
| C8 | 履约推进(需 Celery beat,或手动见 F6) | 状态依次 备餐→配送中→已送达 | □ |

> ⚠️ 已知文案缺陷(非 bug):订单抽屉里履约态会显示**英文** `preparing/delivering/delivered`
> (Orders.tsx 的中文映射只覆盖到 paid/退款等)。状态机本身正确。要我补中文映射可以说一声。

## D. agentic 组单(P3)· 前端(依赖 LLM,较慢/偶发)

| # | 输入 | 预期 | □ |
|---|------|------|---|
| D1 | `帮我组个30块的奶茶套餐` | agent 多步:找店→看菜单→凑单→加购;最后自然语言回复(门店+套餐+合计+剩余预算);**不自动下单**,购物车里出现组好的套餐 | □ |
| D2 | 先按 C 下并支付一单,再输入 `再来一单` | 按上次订单重组购物车,文本说明重组了哪家、哪些 | □ |
| D3 | `附近快餐凑个40块的` | 组主食+饮品,总价 ≤40 | □ |

> LLM 网关慢且偶发(单次 5–40s,可能把 找店/看菜单/凑单 循环两遍再收敛)—— 只要最终购物车正确即算过。
> 若长时间无响应,多半是网关抽风,重试即可。

## E. 交易安全 & 边界

| # | 操作 | 预期 | □ |
|---|------|------|---|
| E1 | 未登录直接调加购(退出后)或看 API | 提示「请先登录」,不能操作 | □ |
| E2 | 伪造支付回调(命令行):`curl -X POST http://127.0.0.1:9000/api/payments/webhook -H "x-signature: deadbeef" -d '{"event":"payment.succeeded","payment_ref":"x"}'` | 返回 `ok:false`(验签失败),订单**不会**变已支付 | □ |
| E3 | 幂等 / 库存(建议走 F3 脚本) | 同 idempotency_key 重复下单返回同一订单;库存原子预占 | □ |

## F. 命令行验证(最稳,不依赖前端/LLM;可复现)

| # | 命令 | 预期 | □ |
|---|------|------|---|
| F1 | `PYTHONPATH=. python scripts/verify_nearby.py "附近奶茶店30分钟送到" --now-hour 14` | 召回 7 → 精确半径过滤 1 → 返回 **6 家** | □ |
| F2 | `... verify_nearby.py "附近奶茶店30分钟送到" --now-hour 21` | 返回 **8 家**(两家晚开店此时营业)→ 证明营业中是真时间过滤 | □ |
| F3 | `PYTHONPATH=. python scripts/verify_instant_order_flow.py` | 组单→下单(库存预占/重算价/幂等)→支付(验签/伪造被拒)→履约(备餐→配送→送达)全绿 ✅ | □ |
| F4 | `PYTHONPATH=. python scripts/verify_p3_tools.py` | assemble(¥40→主食+饮品)+ reorder 全绿 ✅ | □ |
| F5 | `PYTHONPATH=. python scripts/verify_instant_order.py "附近快餐 半小时送到 人均30以内"` | 路由到 instant_order_agent + 返回达标快餐店 | □ |
| F6 | 手动推进履约:`curl -X POST http://127.0.0.1:9000/api/orders/<订单号>/advance`(连点 3 次) | 依次返回 preparing→delivering→delivered | □ |
| F7 | `PYTHONPATH=. python scripts/eval_instant_order.py` | **21/21** + 优雅降级 **2/2**(now_hour=14 固定) | □ |

## G. 既有购物功能回归(顺带验)

| # | 输入/操作 | 预期 | □ |
|---|------|------|---|
| G1 | `帮我找男士衬衫` / `500 以内的跑步鞋` | 商品卡,预算/品类命中 | □ |
| G2 | `送女朋友的生日礼物` | 礼物场景,不出奶茶/食品等被排除品类 | □ |
| G3 | `面霜 300 以内`(若无解) | 带标注的放宽结果,而非空 | □ |
| G4 | 商品加购 → 结算 → 支付 | 与秒送同一套购物车/订单/支付 | □ |
| G5 | `PYTHONPATH=. python scripts/eval_recommendation.py` | 既有推荐 eval 通过率(参考 96.6%) | □ |
| G6 | `PYTHONPATH=. python -m pytest tests/ -q` | 测试通过(个别 LLM 相关用例偶发,孤立重跑) | □ |

## H. 已知限制(测到这些属正常,不是 bug)

- [ ] 履约态在订单 UI 显示英文(见 C8 注)。
- [ ] mock geo/ETA/门店,不接地图与真实骑手调度(设计如此)。
- [ ] 深夜(约 0–4 点)查「附近X」:正例 24h 营业仍可召回;但**夜宵负例(20:00-次日04:00)在 02:00 这类跨零点时刻不会被召回**——已知的跨零点营业判断限制。
- [ ] LLM 组单(D 组)慢且偶发循环;确定性路径(A/C/F)不受影响。

---

## 问题记录(测出来的写这里)

| 用例# | 输入/操作 | 实际结果 | 备注 |
|-------|-----------|----------|------|
|       |           |          |      |
