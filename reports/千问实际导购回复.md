9、帮我找护肤品，要口碑好的

追问：

1/2你的肤质是？   下个问题->(可点击跳转下一问题)

油皮/混油(可点击)   干皮/混干(可点击)   敏感肌(可点击)    不太清楚(可点击)

2/2 主要想解决什么问题？   重选上题->(可点击跳转上一问题)

补水保湿(可点击)    控油祛痘(可点击)    抗老紧致(可点击)   提亮肤色(可点击)  

已选 油皮/混油(若点击了重选上题，则这里撤回已选展示)

确认(可点击)



全部选择完毕，则会自动输入

油皮/混油， 补水保湿



然后回复：

油皮/混油皮想补水保湿，选对护肤品很重要！结合你的肤质，我给你推荐几款口碑好、控油不闷痘的护肤品：

🌟 口碑爆款推荐

谷雨白千水乳 是油皮圈的"老网红"了，控油祛痘效果很能打，补水也不拉胯，性价比超高，很多油皮兄弟用了都说清爽不油腻～

[商品卡片]

🇯🇵 日系温和之选

珂润水乳套装 主打敏感肌友好，控油款质地清爽，补水保湿同时不会给皮肤增加负担，混油皮用起来很舒服，回购率很高！

[商品卡片]

💪 男士专属推荐

资生堂UNO乳液 专为男士设计，控油效果一绝，质地轻薄好吸收，夏天用也完全不闷，补水保湿一步到位，口碑一直很稳～

[商品卡片]

你更倾向国货性价比款还是日系温和款？或者想要一整套水乳+洁面？告诉我可以帮你进一步筛选～



 用户输入"帮我找护肤品，要口碑好的"后，系统应该追问肤质、需求等信息（带可点击选项），但当前追问从未触发。原因：mock 数据只有 6 个护肤品，_count_candidates <= 20 阈值导致直接跳过追问。且追问以纯文本渲染，无可点击选项。



     需要解决的问题
    
     问题 1：追问从不触发
    
     - 根因：clarification_engine.py:153 — candidates <= 20 直接返回不追问，mock 数据 6 个护肤品永远低于阈值
     - 修复：改为「候选数 > 阈值 OR 有高优先级缺失字段」时追问
    
     问题 2：追问无可点击选项
    
     - 根因：QUESTION_TEMPLATES 只有纯文本 question，无 options；SSE 事件只传 explanation 字符串；前端 ChatMessage 无 options 字段   
     - 修复：后端返回结构化 {question, options}，前端渲染为可点击按钮
    
     问题 3：asked_fields 未追踪
    
     - 根因：shopping_graph.py:92 — asked = [] 硬编码，State 无 asked_fields 字段
     - 修复：State 添加 asked_fields: list，在 should_clarify 节点中传递和更新
    
     问题 4：追问后无循环
    
     - 根因：clarify 节点直接到 END，用户回答后需重新发消息
     - 修复：clarify → END 不变（前端用户回答会触发新消息重新走完整流程），但需确保 asked_fields 跨轮次持久化
    
     修改文件
    
     1. src/graph/state.py — 添加 asked_fields 字段
    
     asked_fields: list  # 追问过的字段列表
    
     2. src/agents/clarification_engine.py — 核心修改
    
     2a. QUESTION_TEMPLATES 增加 options 和 category 条件
     - 每个模板增加 options: list[str] 字段（可点击选项）
     - 增加 applies_to_category 字段（仅特定品类显示）
     - 新增 skin_type 和 concerns 模板（护肤品类专用）
    
     2b. 修改追问触发逻辑
     - 移除 candidates <= 20 硬停止
     - 改为：如果 _select_question 返回了高优先级问题（priority > 0.3），就追问
     - 保留 round_num >= 3 上限
    
     2c. _select_question 返回结构化数据
     - 返回 {"field": str, "question": str, "options": list[str]} 而非纯字符串
    
     2d. should_clarify 返回增加 options
     - 返回 {"should_ask": bool, "question": str, "options": list[str], ...}
    
     3. src/graph/shopping_graph.py — 传递 options 和 asked_fields
    
     3a. node_should_clarify
     - 从 state 读取 asked_fields（默认 []）
     - 将 asked_fields 传给 should_clarify
     - 追问时返回 asked_fields: [..., new_field]
    
     3b. node_clarify
     - 返回 {"explanation": question, "clarification_options": options}
    
     3c. run_shopping_stream
     - clarification SSE 事件增加 options 字段
     - {"event": "clarification", "data": {"explanation": "...", "options": [...]}}
    
     4. frontend/src/hooks/useChatStream.ts — ChatMessage 增加 options
    
     export interface ChatMessage {
       role: "user" | "assistant";
       content: string;
       products?: Product[];
       options?: string[];  // 追问可点击选项
       isLoading?: boolean;
     }
     - handleSSEEvent 的 clarification case 解析 data.options
     - 点击选项时，将选项文本作为用户消息发送
    
     5. frontend/src/components/ChatBox.tsx — 渲染可点击选项
    
     - MessageBubble 中，当 message.options 存在时渲染按钮组
     - 每个按钮点击后调用 sendMessage(选项文本)
     - 按钮样式：横向排列，圆角，蓝色边框
    
     验证
    
     - 输入"帮我找护肤品，要口碑好的"→ 应追问"你的肤质是？"并显示选项按钮
     - 点击选项 → 选项文本作为用户消息发送 → 继续推荐流程
     - 运行 pytest tests/ 确保无回归


```
以下是 "帮我找护肤品，要口碑好的" 完整后端流程：

  ---
  全流程概览

  用户消息 → 输入校验 → classify_intent ─┬→ extract_entities ──→ should_clarify → [追问] → END
                                          │
                                          └→ recall_memory    ──↗

  （追问回答后，第二次进入）→ extract_entities(解析JSON) → should_clarify → hybrid_retrieve → promotion_calculate → rank → explain → 
  END

  ---
  第一轮：追问流程

  1. API 入口 src/api/chat.py:46

  POST /api/chat，body: {"message": "帮我找护肤品，要口碑好的", "session_id": "xxx"}

  validate_input() 做三重校验：长度 ≤500、12 种 prompt injection 正则、意图白名单。通过后调用 run_shopping_stream()。

  2. 意图分类 src/router/intent_classifier.py:19

  快速路径 — Semantic Router（src/router/semantic_router.py:46）：
  - 用 bge-m3 将 query 编码为 1024 维向量
  - 在 Qdrant intent_samples 集合中搜索最近邻（75 个样本，5 种意图各 15 个）
  - 阈值 0.80，"帮我找护肤品" 与 search 样本语义相似度高（~0.85+），命中 → ("search", 0.85, "semantic")

  慢路径 — LLM Fallback（src/router/llm_router.py:20）：
  System: 你是意图分类器。从用户输入中判断意图，返回 JSON：
  {"intent": "search|compare|recommend|detail|order", "confidence": 0.0~1.0}
  意图说明：
  - search: 搜索/查找商品
  - compare: 比价/对比不同平台价格
  - recommend: 请求推荐
  - detail: 查看商品详情/评价
  - order: 下单/购买
  只输出 JSON，不要其他文字。
  仅当 Semantic Router 置信度 < 0.80 时触发。

  3. 实体抽取 + 记忆召回（并行）

  extract_entities src/agents/entity_extractor.py:48

  LLM 完整调用：
  System: 你是实体提取器。从用户输入中抽取以下字段，返回 JSON：
  {
    "category": "品类或null",
    "price_min": 数字或null,
    "price_max": 数字或null,
    "brand": "品牌或null",
    "scenario": "场景或null",
    "quantity": 数字或null,
    "preference": "偏好关键词或null",
    "skin_type": "肤质或null（如油皮、干皮、敏感肌，仅护肤品类）",
    "concerns": "护肤需求或null（如补水保湿、控油祛痘、抗老紧致，仅护肤品类）",
    "ambiguous": true/false,
    "ambiguous_fields": ["不确定的字段"]
  }
  品类只限：护肤、奶茶、数码、服饰、食品、家居、母婴、运动
  preference：用户对商品品质的偏好描述，如'口碑好'、'销量高'、'大牌'、'便宜'、'轻薄'等。提取原词，不要改写。
  skin_type/concerns：仅当品类是护肤时才提取，其他品类设为null。
  歧义标记：当实体含义不确定时设为 true（如'苹果'可能是水果或手机），
  并把不确定的字段名加入 ambiguous_fields。
  只输出 JSON，不要其他文字。

  User: 帮我找护肤品，要口碑好的

  预期输出：{"category": "护肤", "preference": "口碑好", "skin_type": null, "concerns": null, "ambiguous": false, ...}

  recall_memory src/memory/memory_retriever.py:95

  should_recall() 检查两类触发词：
  - 指代词：["上次", "那个", "之前", "之前看的", ...]
  - 品类跳变：current_category != prev_category

  本次 query 无指代词且无品类跳变 → False，不触发记忆召回。

  4. 追问判断 src/agents/clarification_engine.py:178

  should_clarify(entities, asked_fields=[], round_num=0) 评估缺失字段优先级：

  priority = missing_degree × discrimination_power × priority_weight

  ┌───────────┬────────┬─────────────────┬──────────┬──────────────────────┐
  │   字段    │ weight │ discrimination  │ priority │         问题         │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ skin_type │ 0.95   │ 0.8 (hardcoded) │ 0.76     │ 你的肤质是？         │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ concerns  │ 0.9    │ 0.8 (hardcoded) │ 0.72     │ 主要想解决什么问题？ │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ price_max │ 0.9    │ ~0.6            │ ~0.54    │ 你的预算大概是多少？ │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ brand     │ 0.7    │ varies          │ varies   │ 有偏好的品牌吗？     │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ scenario  │ 0.6    │ ~0.6            │ ~0.36    │ —                    │
  ├───────────┼────────┼─────────────────┼──────────┼──────────────────────┤
  │ quantity  │ 0.4    │ ~0.4            │ ~0.16    │ —                    │
  └───────────┴────────┴─────────────────┴──────────┴──────────────────────┘

  返回 should_ask: true，批量 3 个问题：skin_type、concerns、price_max。

  5. 前端渲染追问

  SSE 事件：event: clarification\ndata: {"explanation": "...", "questions": [{field, question, options}, ...]}

  前端 ClarificationForm 组件以步骤式表单展示，用户逐题选择后提交 JSON。

  ---
  第二轮：追问回答后的推荐流程

  用户提交 {"price_max":"500-1000","skin_type":"油皮/混油","concerns":"补水保湿"}，前端显示 "500-1000，油皮/混油，补水保湿"。        

  6. JSON 答案解析 src/graph/shopping_graph.py:121

  node_extract_entities 检测到 JSON 输入，调用 _normalize_answer_values() 做值转换：
  - "500-1000" → price_max: 1000
  - "油皮/混油" → skin_type: "油皮/混油"
  - "补水保湿" → concerns: "补水保湿"

  合并到 checkpoint 中的 entities（保留 category: "护肤"）。

  7. 再次追问判断

  asked_fields 从 checkpoint 恢复为 ["price_max", "skin_type", "concerns"]，这些字段被跳过。若无更多高优先级问题 → should_ask:       
  false，进入检索。

  8. 混合检索 src/retrieval/hybrid_retriever.py:31

  向量编码：bge-m3 将原始 query 编码为 1024 维向量（Ollama http://localhost:11505）

  Filter 构建 src/retrieval/filter_builder.py:10：
  Filter(must=[
      FieldCondition(key="category", match=MatchValue(value="护肤")),
      FieldCondition(key="price", range=Range(lte=1000)),  # 如果 price_max 已设置
  ])

  Qdrant 查询 src/retrieval/vector_store.py:50：
  await client.query_points(
      collection_name="products",
      query=query_vector,  # 1024-dim bge-m3
      query_filter=filter,
      limit=10
  )
  返回 top-10 结果，每个包含 score（余弦相似度）和 payload（商品元数据）。

  9. 促销计算 src/tools/promotion_calculator.py:136

  对每个搜索结果计算促销：
  - threshold_discount：满减（如满 200 减 30）
  - step_discount：阶梯折扣（如买 2 件享 8 折）
  - abnormal_shipping：运费陷阱 / 价格异常检测

  返回 final_price、promo_desc、suggest_message。

  10. 多目标排序 src/agents/ranker.py:132

  5 个维度加权求和：

  DEFAULT_WEIGHTS = {
      "relevance": 0.35,      # Qdrant 余弦相似度
      "price": 0.25,          # 价格反向归一化
      "reputation": 0.15,     # 口碑/销量
      "timeliness": 0.10,     # 平台时效性 (jd=0.9, tb=0.6, pdd=0.5)
      "personalization": 0.15, # 品牌/价格偏好匹配
  }

  动态权重调整 _adjust_weights()：
  - preference 含 "口碑" → reputation += 0.15，price -= 0.075，timeliness -= 0.075
  - 调整后归一化：reputation 从 0.15 升至 ~0.28

  后过滤：
  - filter_by_scenario()：场景过滤（送礼排除食品/家居等）
  - 预算过滤：final_price > price_max 的商品移除

  11. 推荐解释 src/agents/explainer.py:84

  结构化理由 generate_reason()：取 top-2 排序维度生成模板文本

  LLM 润色 polish_reason()：
  System: 你是推荐理由润色助手。输出 JSON: {"reason": "润色后的理由"}

  User: 请将以下推荐理由润色为自然、亲切的中文，不超过50字：
  商品：{name}
  特点：{features}
  原始理由：{structured_reason}
  只输出润色后的理由，不要其他文字。

  最终输出格式：
  为你推荐：
  1. 雅诗兰黛小棕瓶 [京东] — ¥580
     推荐理由：口碑出众的精华，油皮适用，补水保湿效果好

  2. ...

  12. 记忆写入

  write_chunk() 异步写入用户专属 Qdrant 集合（fire-and-forget），用于未来 "上次那个" 等指代召回。

  ---
  涉及的 LLM 调用汇总

  ┌──────────────────┬─────────────────────────────────────┬───────────────────┬────────────────────┐
  │       节点       │                函数                 │     调用时机      │    Prompt 核心     │
  ├──────────────────┼─────────────────────────────────────┼───────────────────┼────────────────────┤
  │ classify_intent  │ llm_router.classify()               │ SR 置信度不足时   │ 意图 5 分类        │
  ├──────────────────┼─────────────────────────────────────┼───────────────────┼────────────────────┤
  │ extract_entities │ entity_extractor.extract_entities() │ 每次              │ 实体 12 字段抽取   │
  ├──────────────────┼─────────────────────────────────────┼───────────────────┼────────────────────┤
  │ disambiguate     │ disambiguator.disambiguate()        │ ambiguous=true 时 │ 消歧               │
  ├──────────────────┼─────────────────────────────────────┼───────────────────┼────────────────────┤
  │ polish_reason    │ explainer.polish_reason()           │ top 3 商品各一次  │ 推荐理由润色 ≤50字 │
  └──────────────────┴─────────────────────────────────────┴───────────────────┴────────────────────┘

  涉及的 Embedding 调用

  ┌──────────────────┬──────────────────────┬────────┬──────┐
  │      调用方      │         用途         │  模型  │ 维度 │
  ├──────────────────┼──────────────────────┼────────┼──────┤
  │ Semantic Router  │ query → intent 向量  │ bge-m3 │ 1024 │
  ├──────────────────┼──────────────────────┼────────┼──────┤
  │ Hybrid Retriever │ query → 检索向量     │ bge-m3 │ 1024 │
  ├──────────────────┼──────────────────────┼────────┼──────┤
  │ Memory Retriever │ query → 记忆检索向量 │ bge-m3 │ 1024 │
  ├──────────────────┼──────────────────────┼────────┼──────┤
  │ Memory Writer    │ chunk → 记忆写入向量 │ bge-m3 │ 1024 │
  └──────────────────┴──────────────────────┴────────┴──────┘
```







10、我要买雅诗兰黛小棕瓶

回复：

🌟 雅诗兰黛小棕瓶购买指南

小棕瓶是雅诗兰黛的王牌精华，第七代主打修护+抗老+保湿，熬夜党、初老肌的必备单品！现在正好618大促，入手时机很合适~

按容量怎么选？

1. 想先试试/出差便携 → 小样装

7ml小样很适合尝鲜或者旅行带着，多支装凑起来也很划算。

[商品卡片]

2. 日常使用 → 50ml标准装

最经典的规格，正常使用2-3个月，性价比最高的入门选择。

[商品卡片]

3. 囤货党/重度用户 → 100ml大容量

单价更低，适合长期用、用量大的姐妹，一次买够省心省事。

[商品卡片]

温馨提示：
- 小棕瓶早晚都能用，但晚上修护效果更好
- 搭配小棕瓶眼霜一起用，眼周+全脸双重抗老
- 现在618期间记得先领消费券再下单，能再省一笔！

你是第一次买还是回购？我可以帮你看看有没有更划算的套装组合~



