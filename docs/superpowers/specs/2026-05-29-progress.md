# ShopMesh 开发进度

## 已完成

### 多 Agent 架构精简（2026-05-28）
- 5 agent → 2 agent（search_recommend_agent + detail_compare_agent）
- order_agent 降级为硬编码分支
- 合并 4 个 prompt 文件为 2 个，支持双 response_type
- 更新 config.yaml、agent_config、task_templates、orchestrator、response_schemas、specialized_agents、multi_agent_graph、stream_utils、agent_state
- 测试全部通过（366 passed，31 failed 均为缺少外部服务的预存问题）
- 已提交推送：`2ebbae5`

### SaaS 路线图设计（2026-05-28~29）
- 8 阶段路线图，安全体系独立为 Phase 5
- 技术选型确定（RabbitMQ 替代 Redis 做 broker）
- 已提交推送：`826d0a1`

### Phase 1：认证鉴权 + 多租户隔离（2026-05-29）✅

**P1-T1：JWT 认证模块** `f1537f6`
- 新增 `src/auth/jwt.py`：PyJWT token 生成/验证 + bcrypt 密码哈希
- 新增 `src/auth/dependencies.py`：`get_current_user` FastAPI 依赖
- 新增 `src/auth/router.py`：注册/登录/刷新/me 四个 API
- 新增 `User` 模型（users 表）
- 依赖：PyJWT + bcrypt（passlib 与 bcrypt 5.x 不兼容，改用原生 bcrypt）

**P1-T2：多租户数据改造** `4510666`
- 新增 `src/auth/context.py`：contextvars 传播 tenant_id / user_id
- 新增 `src/auth/middleware.py`：TenantMiddleware 从 JWT 提取 tenant_id
- SQLModel 表加 tenant_id：UserProfile / SessionRecord / Order / ConversationMessage
- Redis key 加 tenant_id 前缀：`tenant:{tid}:session:{sid}:*`
- Qdrant memory collection 加 tenant_id payload index + filter

**P1-T3：Alembic 数据库迁移** `e43bdb3`
- 初始化 alembic，env.py 从 config.yaml 读取 DB URL
- 初始迁移脚本：7 张表全部创建
- `scripts/migrate.py` + `scripts/entrypoint.sh`（Docker 入口自动迁移）

**P1-T4：Docker Compose** `afdab10`
- Dockerfile：Python 3.11-slim，entrypoint 自动 migrate + uvicorn
- docker-compose.yml：postgres + redis + qdrant + app，healthcheck，volume 持久化
- config.yaml 服务 URL 改用 `${ENV:-default}` 语法

**P1-T5：测试验证** `4b1fc01`
- 11 个 auth 测试全通过（注册/登录/me/刷新/租户隔离）
- 现有测试无回归（295 passed，15 failed 均为预存外部服务问题）
- SQLite in-memory 测试 DB，无需 PostgreSQL

### Phase 2：消息队列 + 异步任务（2026-05-29）✅

**P2-T1：Celery + RabbitMQ 集成** `4ad90f5`
- 新增 `src/tasks/` 模块：celery_app.py / memory_tasks.py / cleanup_tasks.py
- RabbitMQ broker + Redis result backend
- Docker Compose 新增 rabbitmq / celery-worker / celery-beat 三个服务
- 任务路由：memory / cleanup / default 三个队列
- Celery Beat 每日清理过期向量记忆

**P2-T2：迁移异步任务到 Celery** `cd62048`
- postprocessing.py: asyncio.create_task 全部替换为 Celery .delay()
- 行为追踪端点迁移到 Celery task
- 新增 save_conversation_message / process_behavior_signal 任务
- 主路径保持同步（Redis RPUSH < 1ms），后台任务走 worker

**P2-T3：任务可靠性**（内嵌于 T1/T2 配置）
- task_acks_late=True：完成后再确认，worker 崩溃可重试
- max_retries=3 + default_retry_delay：自动重试 + 指数退避
- soft_time_limit / time_limit：超时控制
- task_reject_on_worker_lost=True：worker 异常时任务回队列

**P2-T4：任务状态查询 API** `eceedde`
- `GET /api/tasks/{task_id}` 查询 Celery 任务状态
- 返回 task_id / status / result / error

## 当前状态

- **分支**：master
- **最新 commit**：`29fdf84`
- **远程仓库**：`git@github.com:KarseChan/ShopMesh.git`
- **下一步**：Phase 3.5 T6-T7（RabbitMQ 异步通信 / 端到端测试）或 Phase 4

## 架构决策（2026-05-29）

**方案 C：先不拆，后面再分。** Phase 1-3 用 Python 全栈推进，Phase 3 后引入 Java Spring Boot 迁移 CRUD/鉴权。

调整后的路线图：
| 阶段 | 内容 | 语言 |
|------|------|------|
| Phase 1 ✅ | 认证鉴权 + 多租户隔离 | Python |
| Phase 2 ✅ | 消息队列 + 异步任务（Celery + RabbitMQ） | Python |
| Phase 3 ✅ | 分布式限流 + API 网关（Redis Lua + Traefik） | Python |
| Phase 3.5（新增） | Java 后端引入 — CRUD/鉴权迁移 | Java + Python |
| Phase 4 | Agent 生产化 — Guardrails + 成本追踪 | Python |
| Phase 5 | 安全体系 — RBAC + Prompt 安全 + 审计 | Java + Python |
| Phase 6 | 评测体系 + 可观测性 | Python |
| Phase 7 | 支付集成 + 商家后台 API | Java |
| Phase 8 | 容器化部署 + CI/CD | Java + Python |

Java 侧技术栈：Spring Boot 3.x / Spring Data JPA / Spring Security + JWT / Flyway / WebClient

### Phase 3：分布式限流 + API 网关（2026-05-29）✅

**P3-T1：Redis 分布式限流** `2cec81d`
- 新增 `src/ratelimit/limiter.py`：Redis Lua 滑动窗口原子操作
- 新增 `src/ratelimit/middleware.py`：FastAPI 中间件，429 + Retry-After
- 三级限流：per-user(60/min) / per-tenant(300/min) / global(1000/min)
- 失败开放策略：Redis 宕机时不阻断请求
- config.yaml 新增 `rate_limiting` 配置段
- 15 个测试全通过

**P3-T2：API 网关（Traefik）** `6004eb8`
- 新增 `traefik/traefik.yml`：v3 静态配置（entrypoints, Docker provider）
- 新增 `traefik/dynamic.yml`：网关级速率限制 + 安全 headers
- docker-compose.yml 新增 traefik 服务，app 改为 expose + labels
- 所有服务加入 shopmesh 网络，确保容器间通信
- config.yaml 新增 `gateway` 配置段
- app 端口改为 expose（仅内部暴露），Traefik 接管外部 80 端口

**P3-T3：API Key 管理** `0569725`
- 新增 `src/auth/apikey.py`：API Key 生成/哈希/验证（bcrypt）
- 新增 `src/auth/apikey_router.py`：CRUD 端点（创建/列表/吊销）
- 新增 `ApiKey` 模型（api_keys 表）+ Alembic 迁移
- `dependencies.py` 新增 `get_current_user_any`（JWT + API Key 双认证）
- `TenantMiddleware` 支持 X-API-Key header 提取 tenant_id
- `/api/auth/me` 端点同时接受 JWT 和 API Key
- 20 个测试全通过，auth 测试无回归

### Phase 3.5：Java 后端引入 — Strangler Fig 渐进式迁移（2026-05-29）🔄

**P3.5-T1：Spring Boot 项目骨架** `f0c3f49`
- 新增 `shopmesh-java/`：Spring Boot 3.4.5 + Java 17（Maven）
- pom.xml：Spring Security / Data JPA / Flyway / AMQP / jjwt
- Flyway V1 迁移脚本：从 Alembic 转化的全量建表 SQL
- User / ApiKey JPA 实体 + Repository
- SecurityConfig / TenantContext / TenantFilter / GlobalExceptionHandler
- HealthController：/api/health 健康检查
- Dockerfile：多阶段构建（Maven → JRE）
- docker-compose.yml：新增 java-api 服务 + python-agent 服务
- Traefik 路由分流：/api/auth → Java, /api/chat → Python

**P3.5-T2：RSA JWT + JWKS** `6972774`
- 新增 JwtProvider：RSA-2048 密钥对管理 + RS256 签名
- 新增 JwksController：GET /.well-known/jwks.json 暴露公钥
- 新增 JwtAuthFilter：Spring Security JWT 验证过滤器
- 新增 AuthController：register/login/refresh/me 四个 API
- 新增 AuthService：认证业务逻辑
- 12 个 Java 测试全通过（Auth 9 + JWKS 2 + Health 1）

**P3.5-T3：Java API Key CRUD** `cba0f37`
- 新增 ApiKeyService：Key 生成（sk_live_*）/ 列表 / 吊销
- 新增 ApiKeyController：POST/GET/DELETE /api/auth/api-keys
- 修复 TenantFilter 顺序：注册到 Security filter chain
- 5 个 API Key 测试 + 17 个 Java 测试全通过

**P3.5-T4：Python JWT 改造（JWKS 客户端）** `29fdf84`
- 新增 `src/auth/jwks_client.py`：JWKS 公钥拉取 + 缓存 + 定期刷新
- 修改 `src/auth/jwt.py`：`decode_token` 改为 JWKS 验证（RS256），HS256 降级兜底
- Python 不再签发 JWT（Java 为唯一签发方），保留 create_* 用于测试
- 46 个 Python 测试全通过

**P3.5-T5：Traefik 路由分流**（已内嵌于 T1）
- `traefik/dynamic.yml`：/api/auth → Java, /api/chat → Python 精确分流
- 无代理开销，Agent 请求直达 Python

## 技术选型（已确定）

| 用途 | 选型 |
|------|------|
| JWT | PyJWT |
| 密码哈希 | bcrypt（原生） |
| OAuth2 | FastAPI 内置 |
| 数据库迁移 | Alembic |
| 消息队列 | Celery + RabbitMQ（Phase 2） |
| 限流 | Redis Lua（Phase 3） |
| API 网关 | Traefik v3（Phase 3） |
| 监控 | Prometheus + Grafana（Phase 6） |
| 支付 | wechatpayv3（Phase 7） |
| CI/CD | GitHub Actions + ruff + mypy（Phase 8） |

## 路线图文件

- `docs/superpowers/specs/2026-05-28-saas-roadmap-design.md` — 完整 8 阶段路线图
- `docs/superpowers/specs/2026-05-28-multi-agent-refactor-design.md` — 多 Agent 重构 spec
